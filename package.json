{
  "name": "applypilot-server",
  "version": "1.0.0",
  "description": "ApplyPilot bundle server — push code updates to client extension",
  "main": "server.js",
  "scripts": {
  "start": "node server.js",
  "dev": "node --watch server.js",
  "build": "echo \"No build step required\""
},
  "dependencies": {
    "cors": "^2.8.6",
    "express": "^4.22.1"
  }
}
